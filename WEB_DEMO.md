# PDF 테이블 검색 웹 데모 📊

Streamlit 기반 PDF 테이블 검색 웹 인터페이스

## 기능

- ✅ **다중 PDF 업로드**: 여러 PDF 파일을 한 번에 업로드
- ✅ **자연어 검색**: 한국어/영어 자연어로 테이블 검색
- ✅ **진행 상태 표시**: 단계별 진행 상황 실시간 확인
- ✅ **캐싱**: 한 번 변환 후 여러 번 검색 (빠름!)
- ✅ **초기화**: 기존 PDF 삭제 후 새 PDF 업로드
- ✅ **Hybrid Mode**: OCR 기반 복잡한 표 추출

## 실행 방법

### 1. 하이브리드 서버 시작

```bash
opendataloader-pdf-hybrid --port 5002 &
```

### 2. 웹 데모 시작

#### 방법 A: 시작 스크립트
```bash
cd /Users/a452779/Desktop/agent/corp/pdftablesearch
./run_web_demo.sh
```

#### 방법 B: 직접 실행
```bash
cd /Users/a452779/Desktop/agent/corp/pdftablesearch
streamlit run streamlit_app.py --server.port 8501
```

### 3. 웹 브라우저 접속

```
http://localhost:8501
```

## 사용 방법

### 1. PDF 업로드
1. 왼쪽 사이드바에서 "PDF 파일 업로드" 클릭
2. 하나 이상의 PDF 파일 선택
3. "📥 업로드 처리" 버튼 클릭
4. 자동으로 PDF 변환 및 테이블 추출 진행

### 2. 검색
1. 메인 화면에서 검색어 입력
2. "🔎 검색" 버튼 클릭
3. 실시간으로 진행 상태 표시
4. 검색 결과 자동 표시

### 3. 여러 번 검색
- **캐싱 되어 있음**: PDF를 다시 변환할 필요 없음
- **빠른 검색**: 바로 검색 결과 표시

### 4. PDF 초기화
- 사이드바에서 "🔄 전체 초기화" 버튼 클릭
- 모든 PDF 삭제
- 새 PDF 업로드 가능

## 화면 구성

```
┌─────────────────┬─────────────────────────────────────────┐
│                 │                                         │
│  📁 사이드바     │  🔍 메인 화면                         │
│                 │                                         │
│  • PDF 업로드    │  🔍 검색어 입력                         │
│  • 업로드된 목록 │  🔎 검색 버튼                         │
│  • 초기화 버튼  │                                         │
│                 │  📊 검색 결과                           │
│                 │                                         │
│  📖 사용법       │  💡 진행 상태 표시                       │
│                 │                                         │
└─────────────────┴─────────────────────────────────────────┘
```

## 주요 기능 설명

### 진행 상태 표시
- **PDF 변환**: `opendataloader-pdf`로 PDF → Markdown/JSON
- **테이블 추출**: Hybrid Mode로 복잡한 표도 추출
- **임베딩**: SentenceTransformers로 벡터 변환
- **검색**: ChromaDB 벡터 유사도 검색

### 캐싱 기능
- **문서 캐싱**: 업로드된 PDF 변환 결과 저장
- **검색어 재사용**: 같은 PDF로 여러 번 검색 가능
- **빠른 검색**: 재변환 없이 바로 검색 실행

### 초기화 기능
- **전체 초기화**: 모든 PDF 및 벡터 데이터 삭제
- **개별 삭제**: 특정 PDF만 삭제
- **새로운 시작**: 다른 PDF로 테스트 가능

## 검색 예시

### 추천 검색어
- `사업성 평가 결과`
- `정리 재구조화`
- `PF대출 연체율`
- `금융권 지원`
- `부동산 PF 현황`

### 검색 팁
- **구체적일수록 좋음**: "금융권별 PF대출" > "PF대출"
- **키워드 중심**: "연체율", "부실", "정리"
- **복합 검색**: "부동산 PF 연체율 금융권"

## 기술 스택

- **Frontend**: Streamlit
- **Backend**: Python 3.11
- **PDF 처리**: opendataloader-pdf (Hybrid Mode)
- **임베딩**: SentenceTransformers
- **벡터 DB**: ChromaDB
- **LLM**: z.ai API (선택적)

## 제한 사항

- **파일 크기**: 큰 PDF 처리 시 시간 소요
- **메모리**: 많은 PDF 동시 처리 시 메모리 사용량 증가
- **정확도**: 복잡한 표 구조에 따라 추출 정도 달라질 수 있음

## 문제 해결

### 하이브리드 서버 연결 실패
```bash
# 하이브리드 서버 확인
curl http://localhost:5002/health

# 서버가 없으면 시작
opendataloader-pdf-hybrid --port 5002 &
```

### 포트 충돌
```bash
# 다른 포트로 실행
streamlit run streamlit_app.py --server.port 8502
```

## 라이선스

MIT License - 자유롭게 사용 및 수정 가능
